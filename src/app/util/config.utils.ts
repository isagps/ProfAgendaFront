/**
 * URL base da API.
 * 
 * A constante `apiUrl` define o endereço base para as requisições à API. No exemplo fornecido,
 * a URL aponta para o servidor local, na porta 5000, com o caminho `/api`. Esta URL é utilizada
 * como ponto de partida para todas as chamadas de API na aplicação.
 */
export const apiUrl = 'http://127.0.0.1:5000/api';